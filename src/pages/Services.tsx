import { useEffect,useState } from 'react';import { supabase } from '../lib/supabase';import StaticPage from './StaticPage'
export default function Services(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{supabase.from('services').select('*').eq('published',true).order('display_order').then(({data})=>setItems(data??[]))},[]);return <StaticPage eyebrow="SERVIÇOS" title="Posso transformar sua ideia em um projeto digital."><div className="cards-grid">{items.map(x=><article className="info-card" key={x.id}><h3>{x.name}</h3><p>{x.description}</p></article>)}</div></StaticPage>}
