import { useEffect, useState } from 'react'; import { supabase } from '../lib/supabase'; import StaticPage from './StaticPage'
export default function Technologies(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{supabase.from('technologies').select('*').order('display_order').then(({data})=>setItems(data??[]))},[]);return <StaticPage eyebrow="STACK" title="Tecnologias & Ferramentas"><div className="cards-grid">{items.length?items.map(x=><article className="info-card" key={x.id}><h3>{x.name}</h3><p>{x.category}</p><span className="status-pill">{x.level}</span></article>):<p className="muted">As tecnologias aparecerão aqui conforme forem cadastradas no painel.</p>}</div></StaticPage>}
