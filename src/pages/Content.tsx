import { useEffect,useState } from 'react'; import { Link } from 'react-router-dom'; import { supabase } from '../lib/supabase'; import StaticPage from './StaticPage'
export default function Content(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{supabase.from('posts').select('*').eq('published',true).order('published_at',{ascending:false}).then(({data})=>setItems(data??[]))},[]);return <StaticPage eyebrow="CONTEÚDOS" title="Compartilhando o que estou aprendendo"><div className="cards-grid">{items.map(x=><article className="info-card" key={x.id}><span className="status-pill">{x.category}</span><h3>{x.title}</h3><p>{x.summary}</p><Link className="text-link" to={`/conteudos/${x.slug}`}>Ler conteúdo →</Link></article>)}</div></StaticPage>}
