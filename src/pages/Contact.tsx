import { FormEvent,useState } from 'react'; import { supabase } from '../lib/supabase'; import StaticPage from './StaticPage'
export default function Contact(){const [msg,setMsg]=useState('');async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const payload=Object.fromEntries(f.entries());const {error}=await supabase.from('contact_messages').insert(payload);setMsg(error?'Não foi possível enviar. Tente novamente.':'Mensagem enviada com sucesso!');if(!error)e.currentTarget.reset()}
return <StaticPage eyebrow="CONTATO" title="Vamos conversar?"><div className="contact-grid"><div className="prose-card"><p>Tem um projeto, oportunidade ou quer trocar uma ideia sobre tecnologia? Entre em contato comigo.</p></div><form className="form-card" onSubmit={submit}><input name="name" placeholder="Nome" required/><input name="email" type="email" placeholder="E-mail" required/><input name="phone" placeholder="WhatsApp (opcional)"/><input name="subject" placeholder="Assunto"/><select name="contact_type" defaultValue="Projeto"><option>Projeto</option><option>Trabalho</option><option>Parceria</option><option>Dúvida</option><option>Outro</option></select><textarea name="message" placeholder="Mensagem" required rows={6}/><button className="primary-btn">Enviar mensagem</button>{msg&&<p className="form-message">{msg}</p>}</form></div></StaticPage>}
