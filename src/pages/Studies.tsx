import { useEffect,useState } from 'react';import { supabase } from '../lib/supabase';import StaticPage from './StaticPage'
export default function Studies(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{supabase.from('education').select('*').eq('published',true).order('start_date',{ascending:false}).then(({data})=>setItems(data??[]))},[]);return <StaticPage eyebrow="ESTUDOS" title="Minha evolução"><div className="timeline">{items.map(x=><article key={x.id}><time>{x.start_date||x.status}</time><h3>{x.name}</h3><p>{x.institution&&<strong>{x.institution} — </strong>}{x.description}</p><span className="status-pill">{x.category}</span></article>)}</div></StaticPage>}
