import { useEffect,useState } from 'react';import { supabase } from '../lib/supabase';import StaticPage from './StaticPage'
export default function Gallery(){const [items,setItems]=useState<any[]>([]),[selected,setSelected]=useState<any>();useEffect(()=>{supabase.from('gallery').select('*').eq('published',true).order('display_order').then(({data})=>setItems(data??[]))},[]);return <><StaticPage eyebrow="BASTIDORES" title="Construindo na prática"><div className="gallery-grid">{items.map(x=><button key={x.id} onClick={()=>setSelected(x)}><img src={x.image_url} alt={x.alt_text||x.title} loading="lazy"/><span><b>{x.title}</b><small>{x.category}</small></span></button>)}</div>{!items.length&&<p className="muted">Fotos, protótipos e bastidores serão publicados aqui.</p>}</StaticPage>{selected&&<div className="lightbox" onClick={()=>setSelected(null)}><button>×</button><img src={selected.image_url} alt={selected.alt_text||selected.title}/><p>{selected.caption}</p></div>}</>}
