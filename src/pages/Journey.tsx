import { useEffect,useState } from 'react'; import { supabase } from '../lib/supabase'; import StaticPage from './StaticPage'
export default function Journey(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{supabase.from('timeline_events').select('*').eq('published',true).order('event_date').then(({data})=>setItems(data??[]))},[]);return <StaticPage eyebrow="JORNADA" title="Construindo meu caminho na tecnologia"><div className="timeline">{items.map(x=><article key={x.id}><time>{x.event_date}</time><h3>{x.title}</h3><p>{x.description}</p></article>)}{!items.length&&<p className="muted">A timeline crescerá conforme novos marcos forem registrados.</p>}</div></StaticPage>}
